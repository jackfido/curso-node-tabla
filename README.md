# Notes:
This is my first node program

```
Options:
      --help     Show help                                   [boolean]
      --version  Show version number                         [boolean]
  -b, --base     base is the dynamic factor
                                      [number] [required] [default: 0]
  -t, --to       to is the limit to multiply       [number] [required]
  -l, --list     shows result table         [boolean] [default: false]
```